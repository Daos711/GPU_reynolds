"""
reynolds_solver.dynamic — dynamic / squeeze Reynolds solvers.

    solver_dynamic — unsteady Reynolds GPU solver (x', y' velocity terms).
    squeeze        — squeeze-film helpers built on top of solve_reynolds.
"""
