"""
reynolds_solver.tests.legacy — archived tests for removed code paths.

These tests target legacy JFO solvers (moved to cavitation.legacy) and
the `cavitation="jfo"` / `"jfo_ausas"` branches of the old public API,
which have been removed. Kept for reference only.
"""
